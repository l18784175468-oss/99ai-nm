# Node.js 版本要求

## 📋 版本要求

根据项目配置，99AI 项目对 Node.js 的版本要求如下：

### 最低要求
- **Node.js 18.18.0** 或更高版本
- **Node.js 20.9.0** 或更高版本  
- **Node.js 21.1.0** 或更高版本

### 推荐版本
- **Node.js 22.x** (LTS 长期支持版本，推荐)
- **Node.js 20.x** (LTS 长期支持版本)

## 🔍 版本说明

项目在 `admin/package.json` 中明确指定了 Node.js 版本要求：

```json
"engines": {
  "node": "^18.18.0 || ^20.9.0 || >=21.1.0"
}
```

这意味着：
- ✅ Node.js 18.18.0 及以上版本（18.x 系列）
- ✅ Node.js 20.9.0 及以上版本（20.x 系列）
- ✅ Node.js 21.1.0 及以上版本（21.x 系列）
- ✅ Node.js 22.x 及以上版本（22.x 系列）

## 📥 安装方法

### Windows 系统

#### 方法一：官方安装包（推荐）
1. 访问 [Node.js 官网](https://nodejs.org/)
2. 下载 **LTS 版本**（推荐 Node.js 20.x 或 22.x）
3. 运行安装程序，按提示完成安装
4. 验证安装：
   ```powershell
   node -v
   npm -v
   ```

#### 方法二：使用 nvm-windows（推荐用于多版本管理）
1. 下载 [nvm-windows](https://github.com/coreybutler/nvm-windows/releases)
2. 安装 nvm-windows
3. 安装 Node.js：
   ```powershell
   nvm install 22
   nvm use 22
   ```
4. 验证安装：
   ```powershell
   node -v
   ```

### Linux/Mac 系统

#### 使用 nvm（推荐）
```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 重新加载 shell 配置
source ~/.bashrc  # 或 source ~/.zshrc

# 安装 Node.js 22
nvm install 22
nvm use 22

# 验证安装
node -v
npm -v
```

## ✅ 验证安装

安装完成后，在命令行中执行：

```bash
# 检查 Node.js 版本
node -v

# 检查 npm 版本
npm -v

# 检查 pnpm 是否已安装（如果没有，需要安装）
pnpm -v
```

**预期输出示例：**
```
v22.11.0    # Node.js 版本
10.2.4      # npm 版本
9.0.0       # pnpm 版本（如果已安装）
```

## 📦 安装 pnpm

安装 Node.js 后，还需要安装 pnpm（项目使用 pnpm 作为包管理器）：

```bash
npm install -g pnpm
```

验证 pnpm 安装：
```bash
pnpm -v
```

## ⚠️ 注意事项

1. **版本兼容性**
   - 低于 18.18.0 的版本可能无法正常工作
   - 推荐使用 LTS（长期支持）版本，更稳定

2. **版本选择建议**
   - **生产环境**：推荐 Node.js 20.x 或 22.x LTS 版本
   - **开发环境**：可以使用最新版本，但建议使用 LTS 版本

3. **多版本管理**
   - 如果需要在多个项目间切换 Node.js 版本，推荐使用 nvm（Node Version Manager）

4. **Windows 用户**
   - 如果使用 nvm-windows，需要以管理员权限运行 PowerShell

## 🔗 相关链接

- [Node.js 官网](https://nodejs.org/)
- [Node.js 下载页面](https://nodejs.org/zh-cn/download/)
- [nvm-windows GitHub](https://github.com/coreybutler/nvm-windows)
- [nvm GitHub](https://github.com/nvm-sh/nvm)
- [pnpm 官网](https://pnpm.io/)

## 📝 版本要求总结

| 环境 | 最低版本 | 推荐版本 |
|------|---------|---------|
| 开发环境 | Node.js 18.18.0+ | Node.js 22.x LTS |
| 生产环境 | Node.js 18.18.0+ | Node.js 20.x 或 22.x LTS |

